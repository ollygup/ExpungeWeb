For WASM, instead of letting angular.json handle the copying to public directory, it is done via scripts/copy-assets.mjs
this is due to the limitation of ng serve (which will ignore angular.json asset's copying)